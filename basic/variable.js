//The variables should be start with a letter (a to z) or (A to Z), underscore(_) or dollar sign ($)
// after the first letter we can use 0 to 9 digits
// java script is case senstive it means TOM and tom are different variables

a='he is eating'
A='she is reading'
console.log('this a = ',A) // the answer will be 'she is reading'

_name="jhon"

$age=21

console.log(_name,$age) // output is 'jhon 21'

// in the above example a,A,_name,$age are valid variables.
// 007bond, &name are invalid